// getFloor function
// Take the argument x and use [Math.floor] to turn it into an integer without the values after the decimal place.
// return it!

function getFloor(x) {
  return Math.floor(x);
}

floor = getFloor(5.777)
console.log(floor) // rounded integer value - 5