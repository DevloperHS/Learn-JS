// getRandom function
// Inside getRandom, get a random number from the Math.random() function. Then return it!

//The function Math.random generates a number between 0 and 1, not including 1.

function getRandom() {
  return Math.random();
}

rand = getRandom();
console.log(rand); //any random value between 0 & 1