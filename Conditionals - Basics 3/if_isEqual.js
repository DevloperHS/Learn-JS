// Use if when you need to branch based on a condition.
// Check Equal: complete the isEqual function!

//If a is equal to b return true.

function isEqual(a, b) {
    if (a === b) {
        return true
    }
}

console.log(isEqual(5,5)) // true
console.log(isEqual(5,4)) // undefined as no cond for false is defined.
